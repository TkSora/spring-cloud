server:
  port: 8888
spring:
  application:
    name: my-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/TkSora/spring-cloud
          search-paths: config/{application}
      label: master